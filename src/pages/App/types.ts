import {HomeProps} from '../Home/types';
import {InsuranceSummaryProps} from '../InsuranceSummary/types';
import {QuoteHereProps} from '../QuoteHere/types';

export type MyAppPros = {
  Home: HomeProps;
  QuoteHere: QuoteHereProps;
  InsuranceSummary: InsuranceSummaryProps;
};
